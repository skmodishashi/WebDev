let arr = [1,2,3,4,5];

console.log(arr);
console.log(arr.length);
console.log(arr[0]);
arr[0] = 20;
console.log(arr, typeof arr);

// strings are immuntable and arrays are mutable means when we something in array in directly chnages to array but whenn we changes in strings it creates new 

